<h1>Essen</h1>

<%= render 'search' %>

<section>
  <p><%= link_to "Hinzufügen...", new_meal_path %></p>
  <%= render 'meals' %>
  <p><%= link_to "Hinzufügen...", new_meal_path %></p>
</section>

<%= render 'search' %>

